# The specification: DeepAudit

This folder holds the specification Sneferu built DeepAudit from. Every file here is copied byte for byte from Sneferu's own run records. Nothing was retyped or edited.

| File | What it is |
|---|---|
| [`SPECIFICATION.md`](SPECIFICATION.md) | the canonical root specification, revision 5, the file the code run started from |

## Where it came from

- **Code run:** `2026-06-25T13-12-07Z-code-07c0ebd5`. This file is byte-identical to that run's problem statement.
- **Approval:** code runs have no spec-review gate; the run was launched with this specification as its input.
- **Build record:** the implementation and finalizer notes from the same run are in [`../build-record/`](../build-record/).

## Checksums (SHA-256)

| File | SHA-256 |
|---|---|
| `SPECIFICATION.md` | `edd9c4bc3f9de657a52902e5499c60f5c1c5cdac0a005b606a1a977b66752508` |
